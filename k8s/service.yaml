apiVersion: v1
kind: Service
metadata:
  name: ai-cloud-cost-optimizer
  namespace: ai-cost
  labels:
    app: ai-cloud-cost-optimizer
spec:
  selector:
    app: ai-cloud-cost-optimizer
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
