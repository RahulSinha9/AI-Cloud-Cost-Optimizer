apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ai-cloud-cost-optimizer
  namespace: ai-cost
spec:
  selector:
    matchLabels:
      app: ai-cloud-cost-optimizer
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
